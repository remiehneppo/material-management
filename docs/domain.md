# Domain documentation

The source of truth for domain language is [`../CONTEXT.md`](../CONTEXT.md). Architectural decisions live in [`adr/`](adr/).

Frontend code links here but does not duplicate domain invariants. Update the backend context and an ADR before changing a lifecycle, business key, or atomicity guarantee.
